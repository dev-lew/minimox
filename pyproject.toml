[project]
name = "minimox"
version = "0.1.0"
description = "Proxmox infra for small compute."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ansible>=13.6.0",
    "proxmoxer>=2.3.0",
    "requests>=2.33.1",
]
